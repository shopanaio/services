extend schema
  @link(
    url: "https://specs.apollo.dev/federation/v2.7"
    import: [
      "@key"
      "@requires"
      "@provides"
      "@external"
      "@shareable"
      "@override"
      "@inaccessible"
    ]
  )

type Query {
  """Pricing Admin query namespace."""
  pricingQuery: PricingQuery!
}

type Mutation {
  """Pricing Admin mutation namespace."""
  pricingMutation: PricingMutation!
}

"""Scaffold for pricing reads. Add domain fields in dedicated schema files."""
type PricingQuery {
  """Scaffold-only field; remove after adding the first pricing query."""
  _placeholder: Boolean!
    @deprecated(reason: "Replace with pricing query fields")
}

"""Scaffold for pricing commands. Add domain fields in dedicated schema files."""
type PricingMutation {
  """Scaffold-only field; remove after adding the first pricing mutation."""
  _placeholder: Boolean!
    @deprecated(reason: "Replace with pricing mutation fields")
}
