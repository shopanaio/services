/**
 * Entry point for Apps Service
 *
 * Runs apps service in standalone mode.
 * Communicates with other services via NATS.
 *
 * For running multiple services in one process, use @shopana/orchestrator-service instead.
 */
import "./moleculer";
