type Currency {
  code: CurrencyCode!
  title: String!
  isActive: Boolean!
  exchangeRate: Float!
}

input CurrencySetDefaultInput {
  currency: CurrencyCode!
}

type CurrencyUpdatePayload {
  success: Boolean!
  userErrors: [UserError!]!
}
