type Currency {
  code: String!
  title: String!
  isActive: Boolean!
  exchangeRate: Float!
}

input CurrencySetDefaultInput {
  currency: String!
}

type CurrencyUpdatePayload {
  success: Boolean!
  userErrors: [UserError!]!
}
