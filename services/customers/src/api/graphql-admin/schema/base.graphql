extend schema
  @link(
    url: "https://specs.apollo.dev/federation/v2.7"
    import: [
      "@key"
      "@requires"
      "@provides"
      "@external"
      "@shareable"
      "@override"
      "@inaccessible"
    ]
  )

type Query {
  """Customers query namespace."""
  customersQuery: CustomersQuery! @inaccessible
}

type Mutation {
  """Customers mutation namespace."""
  customersMutation: CustomersMutation! @inaccessible
}

"""Internal placeholder for the future admin query namespace."""
type CustomersQuery @inaccessible {
  _empty: Boolean!
}

"""Internal placeholder for the future admin mutation namespace."""
type CustomersMutation @inaccessible {
  _empty: Boolean!
}
