# ---- Types from parent subgraph ----

extend type User @key(fields: "id") {
  id: ID! @external
}

extend type ApiKey @key(fields: "id") {
  id: ID! @external
}

extend type Image @key(fields: "id") {
  id: ID! @external
}
