# ansible-playbook playbooks/woodpecker-workflows/deploy.yml
---
- name: Install config-service on remote host from GHCR
  hosts: shopana
  become: true
  gather_facts: true

  vars:
    # Application configuration
    app_name: woodpecker-workflows
    app_dir: /opt/woodpecker-workflows
    app_data_dir: /opt/woodpecker-workflows/data
    container_port: 3000
    host_http_port: 3300

    # GHCR configuration
    image_tag: "{{ woodpecker_config_service_image_tag }}"
    image_name: "ghcr.io/{{ shopana_ghcr_owner }}/{{ app_name }}:{{ config_service_image_tag }}"
    public_key_signature: "{{ woodpecker_config_service_public_key_signature }}"

  tasks:
    - name: Validate required variables are provided
      ansible.builtin.assert:
        that:
          - shopana_ghcr_owner is defined and shopana_ghcr_owner | length > 0
          - shopana_ghcr_token is defined and shopana_ghcr_token | length > 0
          - public_key_signature is defined and public_key_signature | length > 0
        fail_msg: >-
          Required variables missing. Provide shopana_shopana_ghcr_owner, shopana_shopana_ghcr_token,
          and woodpecker_config_service_public_key via inventory variables.
        success_msg: "All required variables are present"

    - name: Installation block with error handling
      block:
        - name: Show installation information
          ansible.builtin.debug:
            msg:
              - "🚀 Installing {{ app_name }}"
              - "  Image: {{ image_name }}"
              - "  Port: {{ host_http_port }}"
              - "  Directory: {{ app_dir }}"

        - name: Ensure application directories exist on remote host
          ansible.builtin.file:
            path: "{{ item }}"
            state: directory
            mode: "0755"
          loop:
            - "{{ app_dir }}"
            - "{{ app_data_dir }}"

        # Removed step that tried to update a local public key file which may not exist

        - name: Login and pull Docker image from GHCR
          ansible.builtin.shell: |
            echo "{{ shopana_ghcr_token }}" | docker login ghcr.io -u {{ shopana_ghcr_owner }} --password-stdin
            docker pull {{ image_name }}
          no_log: true
          changed_when: true

        - name: Create docker-compose.yml for {{ app_name }}
          ansible.builtin.copy:
            dest: "{{ app_dir }}/docker-compose.yml"
            mode: "0644"
            content: |
              services:
                {{ app_name }}:
                  image: {{ image_name }}
                  container_name: {{ app_name }}
                  restart: unless-stopped
                  ports:
                    - "0.0.0.0:{{ host_http_port }}:{{ container_port }}"
                  volumes:
                    - "{{ app_dir }}/public-key.pem:/app/ci/woodpecker-workflows/public-key.pem:ro"
                  environment:
                    - PORT={{ container_port }}
                    - LOG_LEVEL=debug
                    - NODE_ENV=production
                    - WOODPECKER_GITHUB_TOKEN=${WOODPECKER_CONFIG_SERVICE_GITHUB_API_TOKEN}
                    - CONFIG_SERVICE_PUBLIC_KEY_FILE=${CONFIG_SERVICE_PUBLIC_KEY_FILE}
                    # Signature verification enabled with public key
                    # Only GitHub is supported

        - name: Create .env for {{ app_name }}
          ansible.builtin.copy:
            dest: "{{ app_dir }}/.env"
            mode: "0600"
            content: |
              # {{ app_name }} configuration
              WOODPECKER_CONFIG_SERVICE_GITHUB_API_TOKEN={{ config_service_github_api_token }}
              # Hardcoded relative path to PEM inside working directory
              CONFIG_SERVICE_PUBLIC_KEY_FILE=public-key.pem
              # Only GitHub is supported

        - name: Create public-key.pem file
          ansible.builtin.copy:
            dest: "{{ app_dir }}/public-key.pem"
            mode: "0644"
            content: |
              -----BEGIN PUBLIC KEY-----
              {{ public_key_signature }}
              -----END PUBLIC KEY-----

    - name: Cleanup containers on port {{ host_http_port }}
      ansible.builtin.include_tasks: "{{ root_dir }}/ci/ansible/tasks/cleanup-port-task.yml"
      vars:
        cleanup_port: "{{ host_http_port }}"

    - name: Stop and remove existing {{ app_name }} container if running
      ansible.builtin.command:
        cmd: docker compose down
        chdir: "{{ app_dir }}"
      ignore_errors: true
      changed_when: true

    - name: Wait for port to be released
      ansible.builtin.wait_for:
        port: "{{ host_http_port }}"
        state: stopped
        timeout: 30
      ignore_errors: true

    - name: Additional pause to ensure cleanup
      ansible.builtin.pause:
        seconds: 2

    - name: Start {{ app_name }}
      ansible.builtin.command:
        cmd: docker compose up -d
        chdir: "{{ app_dir }}"
      changed_when: true

    - name: Wait a moment for container to start
      ansible.builtin.pause:
        seconds: 3

    - name: Show startup logs
      ansible.builtin.command:
        cmd: docker compose logs --tail=30
        chdir: "{{ app_dir }}"
      register: startup_logs
      changed_when: false

    - name: Display startup logs
      ansible.builtin.debug:
        var: startup_logs.stdout_lines

    - name: Wait for {{ app_name }} to listen on {{ host_http_port }}
      ansible.builtin.wait_for:
        host: 127.0.0.1
        port: "{{ host_http_port }}"
        delay: 2
        timeout: 60
      ignore_errors: true

    # No need to modify the key inside the container; it's provided via read-only volume

    - name: Show container status
      ansible.builtin.command:
        cmd: docker compose ps
        chdir: "{{ app_dir }}"
      register: compose_ps
      changed_when: false

    - name: Display status
      ansible.builtin.debug:
        var: compose_ps.stdout_lines

    - name: Show access info
      ansible.builtin.debug:
        msg:
          - "✅ {{ app_name }} deployed"
          - ""
          - "🌐 Service Info:"
          - "  Image: {{ image_name }}"
          - "  HTTP: http://{{ ansible_host }}:{{ host_http_port }}"
          - "  Health: http://{{ ansible_host }}:{{ host_http_port }}/healthz"
          - "  Dir: {{ app_dir }}"
          - "  Env: {{ app_dir }}/.env"
          - ""
          - "📊 View Logs:"
          - "  ansible-playbook wp-config-service-logs.yml"
          - "  ansible-playbook wp-config-service-logs.yml -e 'follow=true'"
          - "  ansible-playbook wp-config-service-manage.yml -e 'action=logs'"
          - ""
          - "🔧 Manage Service:"
          - "  ansible-playbook wp-config-service-manage.yml -e 'action=restart'"
          - "  ansible-playbook wp-config-service-manage.yml -e 'action=status'"
          - ""
          - "🧹 Cleanup Port:"
          - "  ansible-playbook cleanup-port.yml -e 'cleanup_port={{ host_http_port }}'"

      rescue:
        - name: Show error details
          ansible.builtin.debug:
            msg:
              - "❌ Installation failed!"
              - "Error: {{ ansible_failed_result.msg | default('Unknown error') }}"
              - ""
              - "Attempting to show current state..."

        - name: Show container status on error
          ansible.builtin.command:
            cmd: docker compose ps
            chdir: "{{ app_dir }}"
          register: error_compose_ps
          changed_when: false
          ignore_errors: true

        - name: Display error container status
          ansible.builtin.debug:
            var: error_compose_ps.stdout_lines
          when: error_compose_ps is defined

        - name: Show container logs on error
          ansible.builtin.command:
            cmd: docker compose logs --tail=50
            chdir: "{{ app_dir }}"
          register: error_logs
          changed_when: false
          ignore_errors: true

        - name: Display error logs
          ansible.builtin.debug:
            var: error_logs.stdout_lines
          when: error_logs is defined and error_logs.stdout_lines is defined

        - name: Show recovery instructions
          ansible.builtin.debug:
            msg:
              - "💡 Recovery options:"
              - "  • Retry: ansible-playbook wp-config-service-install.yml"
              - "  • Clean port: ansible-playbook cleanup-port.yml -e 'cleanup_port={{ host_http_port }}'"
              - "  • Check logs: ssh {{ ansible_host }} 'cd {{ app_dir }} && docker compose logs'"
              - "  • Clean install: ssh {{ ansible_host }} 'rm -rf {{ app_dir }}'"

        - name: Fail with message
          ansible.builtin.fail:
            msg: "Installation failed. See error details above."

      always:
        - name: Logout from GitHub Container Registry
          ansible.builtin.command:
            cmd: docker logout ghcr.io
          changed_when: false
          ignore_errors: true
