# Woodpecker CI Environment Variables Example
# Copy this file to .env and set your actual values before running the playbook
# Usage: source export-env.sh

# Server Configuration
# Public URL where Woodpecker will be accessible (with protocol)
WOODPECKER_HOST=https://ci.example.com

# Admin Users
# Comma-separated list of GitHub usernames that will have admin access
WOODPECKER_ADMIN=your-github-username

# Agent Secret
# Shared secret between server and agents (auto-generated if not set)
# WOODPECKER_AGENT_SECRET=your-secret-here

# ===== Git Provider Configuration =====

# GitHub OAuth (default)
WOODPECKER_GITHUB_CLIENT=your-github-oauth-client-id
WOODPECKER_GITHUB_SECRET=your-github-oauth-client-secret

# Alternative: Gitea
# WOODPECKER_GITEA_URL=https://gitea.example.com
# WOODPECKER_GITEA_CLIENT=your-gitea-oauth-client-id
# WOODPECKER_GITEA_SECRET=your-gitea-oauth-client-secret

# Alternative: GitLab
# WOODPECKER_GITLAB_URL=https://gitlab.com
# WOODPECKER_GITLAB_CLIENT=your-gitlab-oauth-client-id
# WOODPECKER_GITLAB_SECRET=your-gitlab-oauth-client-secret

# ===== How to create GitHub OAuth application =====
#
# 1. Go to GitHub → Settings → Developer settings → OAuth Apps
#    https://github.com/settings/developers
#
# 2. Click "New OAuth App"
#
# 3. Fill in the details:
#    - Application name: Woodpecker CI
#    - Homepage URL: https://ci.example.com (your WOODPECKER_HOST)
#    - Authorization callback URL: https://ci.example.com/authorize
#
# 4. Click "Register application"
#
# 5. Copy the Client ID → set as WOODPECKER_GITHUB_CLIENT
#
# 6. Generate a new client secret → set as WOODPECKER_GITHUB_SECRET
#
# ===== Alternative providers =====
#
# For Gitea:
# 1. Go to Settings → Applications → Manage OAuth2 Applications
# 2. Create new OAuth2 Application
# 3. Redirect URI: https://ci.example.com/authorize
# 4. Copy Client ID and Client Secret
#
# For GitLab:
# 1. Go to User Settings → Applications
# 2. Create new application
# 3. Redirect URI: https://ci.example.com/authorize
# 4. Scopes: api, read_user, read_repository
# 5. Copy Application ID and Secret


# GitHub Container Registry Configuration
# Owner/organization name for GHCR (current: shopanaio)
SHOPANA_GHCR_OWNER=shopanaio

# GitHub Personal Access Token with packages:write permission
# Create at: https://github.com/settings/tokens
# Required scopes: write:packages, read:packages
SHOPANA_GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Docker image tag (optional, default: latest)
# Examples: latest, v1.0.0, dev, staging, production
WOODPECKER_CONFIG_SERVICE_IMAGE_TAG=latest

# Woodpecker CI Configuration Service Settings
# GitHub token for accessing workflow configurations
# Create at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
WOODPECKER_CONFIG_SERVICE_GITHUB_API_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
