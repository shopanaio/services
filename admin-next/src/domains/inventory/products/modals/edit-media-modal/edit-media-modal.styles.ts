import { createStyles } from "antd-style";

export const useStyles = createStyles(() => ({
  container: {
    display: "flex",
    flexDirection: "column",
    gap: 16,
    height: "100%",
    padding: 16,
  },
}));
