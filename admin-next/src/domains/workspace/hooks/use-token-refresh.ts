"use client";

/**
 * @deprecated Import from `@/domains/auth` instead.
 * This file will be removed in a future version.
 *
 * Migration:
 * ```ts
 * // Before
 * import { useTokenRefresh } from "@/domains/workspace/hooks";
 *
 * // After
 * import { useTokenRefresh } from "@/domains/auth";
 * ```
 */
export { useTokenRefresh } from "@/domains/auth";
