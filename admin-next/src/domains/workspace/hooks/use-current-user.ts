"use client";

/**
 * @deprecated Import from `@/domains/auth` instead.
 * This file will be removed in a future version.
 *
 * Migration:
 * ```ts
 * // Before
 * import { useCurrentUser } from "@/domains/workspace/hooks";
 *
 * // After
 * import { useCurrentUser } from "@/domains/auth";
 * ```
 */
export { useCurrentUser } from "@/domains/auth";
