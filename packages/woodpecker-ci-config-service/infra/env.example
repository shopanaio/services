# Woodpecker Server Configuration
WOODPECKER_HOST=http://localhost:8000
WOODPECKER_AGENT_SECRET=change-this-to-random-secret-string

# Admin user (your GitHub username)
WOODPECKER_ADMIN=your-github-username

# GitHub OAuth App credentials
# Create OAuth App at: https://github.com/settings/developers
# Homepage URL: http://localhost:8000
# Authorization callback URL: http://localhost:8000/authorize
WOODPECKER_GITHUB_CLIENT=your-github-oauth-client-id
WOODPECKER_GITHUB_SECRET=your-github-oauth-client-secret

# Gitea Configuration (for docker-compose.gitea.yml)
# Public URL of Gitea web UI (must end with slash)
GITEA_ROOT_URL=http://localhost:3000/
# Host HTTP port mapping for Gitea (change if 3000 busy)
GITEA_HTTP_PORT=3000

# SSH settings for Gitea built-in SSH server
GITEA_SSH_DOMAIN=localhost
# Host SSH port mapping; change if 2222 busy (e.g., 2223)
GITEA_SSH_PORT=2222

# Registration and visibility
GITEA_DISABLE_REGISTRATION=false
GITEA_ALLOW_ONLY_INTERNAL_REGISTRATION=false
GITEA_REQUIRE_SIGNIN_VIEW=false
