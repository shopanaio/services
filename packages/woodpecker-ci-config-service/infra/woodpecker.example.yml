# Woodpecker pipeline template for Shopana.io services
# Woodpecker CI reads this template, which is handled by a Node.js server
# Add this file to the root of your test Gitea repository

skip_clone: true

when:
  - event: [manual]

steps:
  - name: "template"
    image: docker:latest
    commands:
      - echo "Running Woodpecker workflow template"
