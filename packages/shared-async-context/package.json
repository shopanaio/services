{
  "name": "@shopana/shared-async-context",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "AsyncLocalStorage helpers for ephemeral use case context",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "node ../esbuild.js",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.5.4"
  },
  "files": [
    "dist"
  ]
}
