# NATS Configuration Environment Variables
# Copy this file to .env.nats and update with your values

# Environment: dev, staging, production
NATS_ENV=dev

# Server configuration
NATS_SERVER_NAME=nats-dev

# Basic authentication
NATS_USER=shopana
NATS_PASSWORD=shopana_dev_password

# Admin user (for system account)
NATS_ADMIN_PASSWORD=admin_dev_password

# Cluster authentication (for multi-node setup)
NATS_CLUSTER_USER=cluster
NATS_CLUSTER_PASSWORD=cluster_password

# Leafnode authentication (for hybrid cloud setup)
NATS_LEAFNODE_USER=leafnode
NATS_LEAFNODE_PASSWORD=leafnode_password

# Token authentication (alternative to user/password)
# NATS_TOKEN=your_secret_token

# TLS/SSL (for production)
# NATS_TLS_CERT=/path/to/cert.pem
# NATS_TLS_KEY=/path/to/key.pem
# NATS_TLS_CA=/path/to/ca.pem
