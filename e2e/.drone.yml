kind: pipeline
type: docker
name: template

# 10
trigger:
  event:
    - push
  ref:
    exclude:
      - refs/heads/main

clone:
  disable: true

steps:
  - name: 'template'
    image: docker:latest
    commands:
      - sleep 10
      - echo "Template"
