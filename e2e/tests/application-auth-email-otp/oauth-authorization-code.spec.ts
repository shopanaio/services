import { test } from '@fixtures/base.extend';

test.describe('Application email OTP auth — OAuth authorization code', () => {
  test.fixme('OTP signin resumes an authorization-code flow for the target client', async () => {});

  test.fixme('OTP signin cannot complete authorization for a foreign application client', async () => {});

  test.fixme('authorization completion requires consent when consent is enabled', async () => {});

  test.fixme('an OTP cannot be exchanged directly for OAuth tokens', async () => {});
});
