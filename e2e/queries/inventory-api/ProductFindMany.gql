query ProductFindMany($first: Int, $after: String) {
  catalogQuery {
    products(first: $first, after: $after) {
      edges {
        node {
          id
          handle
          publishedAt
          isPublished
          createdAt
          updatedAt
          title
          variantsCount
        }
        cursor
      }
      pageInfo {
        hasNextPage
        hasPreviousPage
        startCursor
        endCursor
      }
      totalCount
    }
  }
}
