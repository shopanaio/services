mutation VariantCreate($input: VariantCreateInput!) {
  catalogMutation {
    variantCreate(input: $input) {
      variant {
        id
        inventoryItem {
          id
          sku
          trackInventory
        }
      }
      userErrors {
        message
        code
      }
    }
  }
}
