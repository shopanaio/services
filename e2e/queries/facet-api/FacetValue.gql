query FacetValue($id: ID!) {
  listingQuery {
    facetValue(id: $id) {
      id
      slug
      label
      sourceHandles
      sortIndex
      enabled
      swatch {
        id
        swatchType
        colorOne
        colorTwo
      }
      facet {
        id
        slug
      }
    }
  }
}
