mutation StoreUpdate($input: StoreUpdateInput!) {
  storeMutation {
    storeUpdate(input: $input) {
      store {
        id
        name
        displayName
        email
        timezone
      }
      userErrors {
        code
        message
        field
      }
    }
  }
}
