query Stores($organizationId: ID!) {
  storeQuery {
    stores(organizationId: $organizationId) {
      id
      name
      displayName
      status
      timezone
      email
      locales
      defaultLocale
      currencyCode
      defaultWeightUnit
      defaultDimensionUnit
      createdAt
      updatedAt
    }
  }
}
