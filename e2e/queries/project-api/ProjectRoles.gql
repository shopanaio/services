query ProjectRoles($slug: String!) {
  projectQuery {
    project(slug: $slug) {
      id
      name
      slug
      roles {
        name
        displayName
        description
        isSystem
        inherits
        permissions {
          resource
          actions
          effect
        }
        createdAt
      }
    }
  }
}
