query Store($slug: String!) {
  storeQuery {
    store(slug: $slug) {
      id
      name
      slug
      status
      timezone
      email
      locales
      currencies
      baseCurrency
      defaultLocale
      defaultCurrency
      defaultWeightUnit
      defaultDimensionUnit
      createdAt
      updatedAt
    }
  }
}
