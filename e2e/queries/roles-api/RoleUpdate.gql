mutation RoleUpdate($input: RoleUpdateInput!) {
  roleMutation {
    roleUpdate(input: $input) {
      role {
        name
        displayName
        description
        isSystem
        permissions {
          resource
          actions
        }
        createdAt
      }
      userErrors {
        code
        message
        field
      }
    }
  }
}
