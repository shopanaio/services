mutation RoleCreate($input: RoleCreateInput!) {
  roleMutation {
    roleCreate(input: $input) {
      role {
        name
        displayName
        description
        isSystem
        permissions {
          resource
          actions
          effect
        }
        createdAt
      }
      userErrors {
        code
        message
        field
      }
    }
  }
}
