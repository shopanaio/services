mutation RoleCreate($input: RoleCreateInput!) {
  roleMutation {
    roleCreate(input: $input) {
      role {
        id
        name
        displayName
        description
        isSystem
        permissions {
          resource
          actions
        }
        createdAt
      }
      userErrors {
        code
        message
        field
      }
    }
  }
}
