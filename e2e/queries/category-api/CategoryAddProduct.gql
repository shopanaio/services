mutation CategoryAddProduct($productId: ID!, $categoryId: ID!) {
  catalogMutation {
    productUpdate(
      productId: $productId
      
      operations: { categories: [{ categoryId: $categoryId, action: ADD }] }
    ) {
      product {
        id
        handle
      }
      userErrors {
        message
        code
        field
      }
    }
  }
}
