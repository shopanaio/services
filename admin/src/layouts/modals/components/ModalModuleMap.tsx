import { ModalTypes } from '@src/layouts/modals/types';

export const ModalModuleMap: Record<ModalTypes, React.ComponentType<any>> = {
  // Add your modal components here
  // Example:
  // [ModalTypes.CONFIRM_DELETE]: ConfirmDeleteModal,
};
