"use client";
import { useEffect, useState } from "react";
import { Alert, App, Button, Descriptions, Flex, Input, Select, Skeleton, Tag, Typography } from "antd";
import { DeleteOutlined, FileProtectOutlined, StopOutlined, UserOutlined } from "@ant-design/icons";
import { CustomerDataRequestStatus, CustomerDataRequestType } from "@/graphql/types";
import { ModalHeader, ModalLayout, useModalStackContext } from "@/layouts/modals";
import { Paper, PaperHeader } from "@/ui-kit/paper";
import { useEntityPicker } from "@/shared/components/entity-picker-modal"; import type { IPickableEntity } from "@/shared/components/entity-picker-modal/types";
import "@/domains/customers/all-customers/picker/customer-picker-config";
import { useCustomerDataRequest, useCustomerDataRequestMutations } from "../../hooks"; import type { CustomerDataRequestModalPayload } from "../../modals";

export function CustomerDataRequestModal() {
  const { message, modal } = App.useApp(); const { payload, pop, forcePop, setDirty } = useModalStackContext(); const value = payload as CustomerDataRequestModalPayload; const isEdit = value.mode === "edit"; const query = useCustomerDataRequest(isEdit ? value.entityId : undefined); const mutations = useCustomerDataRequestMutations(); const [customerId, setCustomerId] = useState(value.customerId ?? ""); const [customerLabel, setCustomerLabel] = useState(value.customerId ?? ""); const [type, setType] = useState(CustomerDataRequestType.Access); const [legalBasis, setLegalBasis] = useState(""); const [dueAt, setDueAt] = useState(""); const [metadata, setMetadata] = useState("{}"); const [cancelReason, setCancelReason] = useState(""); const [dirty, setLocalDirty] = useState(false); const [error, setError] = useState<string | null>(null); const request = query.request;
  useEffect(() => {
    if (!request) return;
    setCustomerId(request.customer.id);
    setCustomerLabel(request.customer.displayName);
    setType(request.type);
    setLegalBasis(request.legalBasis ?? "");
    setDueAt(request.dueAt ? request.dueAt.slice(0, 16) : "");
    setMetadata(JSON.stringify(request.requestMetadata, null, 2));
    setLocalDirty(false);
  }, [request]);
  useEffect(() => setDirty(dirty), [dirty, setDirty]);
  const { openPicker } = useEntityPicker<IPickableEntity>({ entityType: "customer", selectionMode: "single", initialSelection: customerId ? [customerId] : [], onConfirm: (items, ids) => { setCustomerId(ids[0] ?? ""); setCustomerLabel(items[0]?.title ?? ids[0] ?? ""); setLocalDirty(true); } });
  const parseMetadata = () => { try { const value = JSON.parse(metadata); if (!value || Array.isArray(value) || typeof value !== "object") throw new Error(); return value; } catch { setError("Request metadata must be a valid JSON object."); return null; } };
  const save = async () => { setError(null); if (!customerId) return setError("Select a customer."); const requestMetadata = parseMetadata(); if (!requestMetadata) return; const input = { customerId, type, legalBasis: legalBasis.trim() || null, dueAt: dueAt ? new Date(dueAt).toISOString() : null, requestMetadata }; const result = request ? await mutations.updateRequest(request.id, input) : await mutations.createRequest(input); if (!result.dataRequest || result.userErrors.length) return setError(result.userErrors.map((item) => item.message).join(" ") || "Unable to save request"); await value.onSaved?.(); setDirty(false); message.success(request ? "Privacy request updated" : "Privacy request created"); forcePop(); };
  const cancel = async () => { if (!request) return; const result = await mutations.updateRequest(request.id, { cancel: { reason: cancelReason.trim() || null } }); if (!result.dataRequest) return setError(result.userErrors.map((item) => item.message).join(" ") || "Unable to cancel request"); await value.onSaved?.(); message.success("Privacy request cancelled"); forcePop(); };
  const remove = async () => { if (!request) return; const confirmed = await modal.confirm({ title: "Delete privacy request?", okText: "Delete", okButtonProps: { danger: true } }); if (!confirmed) return; const result = await mutations.deleteRequest({ id: request.id }); if (!result.deletedDataRequestId) return setError(result.userErrors.map((item) => item.message).join(" ") || "Unable to delete request"); await value.onSaved?.(); forcePop(); };
  if (isEdit && query.loading && !request) return <ModalLayout name="customer-data-request" headerProps={{ title: "Privacy request", onClose: pop, submitButtonProps: null }}><Skeleton active /></ModalLayout>;
  const terminal = request && [CustomerDataRequestStatus.Completed, CustomerDataRequestStatus.Cancelled, CustomerDataRequestStatus.Rejected].includes(request.status);
  return <ModalLayout name="customer-data-request" header={<ModalHeader title={request ? "Privacy request" : "New privacy request"} onClose={pop} extra={request ? <Tag>{request.status.toLowerCase()}</Tag> : null} submitButtonProps={terminal ? null : { children: request ? "Save" : "Create", loading: mutations.loading, disabled: !!request && !dirty, onClick: save }} />}><Flex vertical gap={12}>{query.error || mutations.error ? <Alert type="error" showIcon message={(query.error ?? mutations.error)?.message} /> : null}{error ? <Alert type="error" showIcon message={error} /> : null}<Paper><PaperHeader title="Request" icon={<FileProtectOutlined />} /><Flex vertical gap="middle"><div><Typography.Text strong>Customer</Typography.Text><Flex gap="small" style={{ marginTop: 8 }}><Input readOnly value={customerLabel} /><Button icon={<UserOutlined />} onClick={openPicker} disabled={!!terminal}>Select</Button></Flex></div><div><Typography.Text strong>Type</Typography.Text><Select value={type} disabled={!!terminal} options={Object.values(CustomerDataRequestType).map((item) => ({ value: item, label: item.toLowerCase() }))} onChange={(item) => { setType(item); setLocalDirty(true); }} style={{ width: "100%", marginTop: 8 }} /></div><div><Typography.Text strong>Legal basis</Typography.Text><Input.TextArea value={legalBasis} disabled={!!terminal} onChange={(event) => { setLegalBasis(event.target.value); setLocalDirty(true); }} rows={3} style={{ marginTop: 8 }} /></div><div><Typography.Text strong>Due at</Typography.Text><Input type="datetime-local" value={dueAt} disabled={!!terminal} onChange={(event) => { setDueAt(event.target.value); setLocalDirty(true); }} style={{ marginTop: 8 }} /></div><div><Typography.Text strong>Metadata (JSON)</Typography.Text><Input.TextArea value={metadata} disabled={!!terminal} onChange={(event) => { setMetadata(event.target.value); setLocalDirty(true); }} rows={8} style={{ marginTop: 8, fontFamily: "monospace" }} /></div></Flex></Paper>{request ? <Paper><PaperHeader title="Processing & result" /><Descriptions column={1} items={[{ key: "requested", label: "Requested", children: new Date(request.requestedAt).toLocaleString() }, { key: "started", label: "Started", children: request.startedAt ? new Date(request.startedAt).toLocaleString() : "—" }, { key: "finished", label: "Finished", children: request.finishedAt ? new Date(request.finishedAt).toLocaleString() : "—" }, { key: "result", label: "Result", children: request.resultFile ? <a href={request.resultFile.url} target="_blank" rel="noreferrer">{request.resultFile.originalName ?? "Open result file"}</a> : "—" }, { key: "rejection", label: "Rejection reason", children: request.rejectionReason ?? "—" }, { key: "idempotency", label: "Idempotency key", children: request.idempotencyKey }]} /></Paper> : null}{request && !terminal ? <Paper><PaperHeader title="Cancel request" icon={<StopOutlined />} /><Input.TextArea value={cancelReason} onChange={(event) => setCancelReason(event.target.value)} placeholder="Optional cancellation reason" rows={3} /><Button danger style={{ marginTop: 12 }} onClick={cancel}>Cancel request</Button></Paper> : null}{request ? <Paper><PaperHeader title="Danger zone" /><Button danger icon={<DeleteOutlined />} onClick={remove}>Delete request</Button></Paper> : null}</Flex></ModalLayout>;
}
