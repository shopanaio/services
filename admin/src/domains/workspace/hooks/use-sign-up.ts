"use client";

/**
 * @deprecated Import from `@/domains/auth` instead.
 * This file will be removed in a future version.
 *
 * Migration:
 * ```ts
 * // Before
 * import { useSignUp } from "@/domains/workspace/hooks";
 *
 * // After
 * import { useSignUp } from "@/domains/auth";
 * ```
 */
export { useSignUp } from "@/domains/auth";
