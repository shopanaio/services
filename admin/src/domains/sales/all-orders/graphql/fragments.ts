import { gql } from "@apollo/client";
export const PAGE_INFO_FIELDS = gql`fragment PageInfoFields on PageInfo { startCursor endCursor hasNextPage hasPreviousPage }`;
export const USER_ERROR_FIELDS = gql`fragment UserErrorFields on OrderUserError { code field message }`;
export const ORDER_LIST_FIELDS = gql`fragment OrderListFields on Order { id version orderNumber status createdAt updatedAt currencyCode customerDetails { firstName lastName email phone } paymentSummary { totalAmount } paymentItem { id status } fulfillments { id status shippingItem { id trackingCode } } shippingMethod { id name } orderItems { id product { id title thumbnailUrl } } }`;
export const ORDER_DETAILS_FIELDS = gql`fragment OrderDetailsFields on Order { ...OrderListFields adminNote externalSystemId displayCurrencyCode displayExchangeRate customer { id displayName email phone } customerDetails { firstName lastName middleName email phone note meta } customerStatistic { ordersCount totalSpent } billingAddress { id firstName lastName company address1 address2 city province postalCode countryCode phone } shippingAddress { id firstName lastName company address1 address2 city province postalCode countryCode phone } paymentMethod { id name } shippingMethod { id name price } orderItems { id price quantity originalQuantity fulfillmentQuantity totalAmount subtotalAmount taxAmount discountAmount productCostPrice weight { value unit } product { id title sku thumbnailUrl } createdAt } productsInfo { id title sku thumbnailUrl } fulfillments { id parentId status createdAt updatedAt orderItems { id quantity product { id title sku thumbnailUrl } } shippingItem { id trackingCode trackingUrl estimatedDeliveryAt shippingMethod { id name price } } } paymentItem { id status amount method { id name } } paymentSummary { subtotalAmount discountAmount shippingAmount taxAmount totalAmount paidAmount } events { id type message actorName createdAt } tags { id name } }`;
