"use client";
import { useCallback, useEffect, useRef, useState } from "react";
import { requestOrders } from "../api/request-orders";
import type { ApiOrder, OrderConnection, OrdersQueryVariables } from "../graphql/operation-types";
export function useOrders(variables: OrdersQueryVariables) { const requestId = useRef(0); const [connection, setConnection] = useState<OrderConnection | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState<Error | null>(null); const execute = useCallback(async () => { const id = ++requestId.current; setLoading(true); setError(null); try { const data = await requestOrders(variables); if (id === requestId.current) setConnection(data.ordersQuery.orders); return data; } catch (cause) { const value = cause instanceof Error ? cause : new Error("Unable to load orders"); if (id === requestId.current) setError(value); throw value; } finally { if (id === requestId.current) setLoading(false); } }, [variables]); useEffect(() => { void execute().catch(() => undefined); }, [execute]); const orders: ApiOrder[] = connection?.edges.map((edge) => edge.node) ?? []; return { orders, connection, totalCount: connection?.totalCount ?? 0, pageInfo: connection?.pageInfo ?? null, loading, error, refetch: execute }; }
