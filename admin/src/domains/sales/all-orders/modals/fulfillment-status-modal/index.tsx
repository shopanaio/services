"use client";
import { useState } from "react";
import { Alert, Input, Typography } from "antd";
import { ModalHeader, ModalLayout, useModalStackContext } from "@/layouts/modals";
import type { OrderFulfillmentStatusModalPayload } from "../../modals";
import { useUpdateFulfillmentStatus } from "../../hooks";
export function OrderFulfillmentStatusModal() { const { payload, pop, forcePop } = useModalStackContext(); const value = payload as OrderFulfillmentStatusModalPayload; const [comment, setComment] = useState(""); const [error, setError] = useState<string | null>(null); const mutation = useUpdateFulfillmentStatus(); const submit = async () => { const result = await mutation.updateFulfillmentStatus({ id: value.orderId, fulfillmentId: value.fulfillmentId, expectedVersion: value.expectedVersion, nextStatus: value.nextStatus, comment }); if (!result.order) return setError(result.userErrors.map((item) => item.message).join(" ")); await value.onSaved?.(); forcePop(); }; return <ModalLayout name="order-fulfillment-status" header={<ModalHeader title="Fulfillment status" onClose={pop} submitButtonProps={{ children: "Confirm", loading: mutation.loading, disabled: value.currentStatus === value.nextStatus, onClick: submit }} />}>{error || mutation.error ? <Alert type="error" showIcon message={error ?? mutation.error?.message} /> : null}<Typography.Paragraph><strong>{value.currentStatus}</strong> → <strong>{value.nextStatus}</strong></Typography.Paragraph><Input.TextArea value={comment} onChange={(event) => setComment(event.target.value)} maxLength={500} placeholder="Optional comment" /></ModalLayout>; }
