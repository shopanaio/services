import { ProductVariantsTable } from '@modules/products/components/variants/TableV2';
import { useListenOptions } from '@modules/products/hooks/useListenOptions/useListenOptions';

export const ProductVariants = ({ refetch }: { refetch: (id: ID) => void }) => {
  useListenOptions();
  return </>;
};
